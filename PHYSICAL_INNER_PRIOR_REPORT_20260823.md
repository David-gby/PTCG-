# CardScope 58×83 mm 物理先验优化报告

日期：2026-08-23  
版本：CardScope v0.8.5  
流水线：`ptcg_outer_inner_pipeline_20260822_physical_v1`

## 1. 已确认的测量定义

- 卡牌物理尺寸：63×88 mm。
- 所有版式的印刷内框尺寸：58×83 mm。
- 58×83 mm 指印刷线的**内沿**，不是线中心或线外沿。
- 标准透视校正平面：630×880 px；因此训练与推理中的名义内框为 580×830 px。
- 输出字段 `coordinate_semantics` 已统一为 `printed_inner_line_inner_edge`。

## 2. 实现方式

1. 新增 `physical_inner_prior.py/json`，在最终内框后计算宽高物理残差。
2. 物理约束不直接覆盖视觉结果：只有残差处于 12–32 px 的中等区间，且分段边缘证据支持移动后的两条边时，才允许小幅修正。
3. 残差超过 32 px 时不进行强拉回，而是标记为可能的外框透视校正或内框检测失败并进入复核。
4. 横向规则在锁定测试出现不稳定，因此正式版中横向只做质量审查，不改变 left/right。
5. 纵向规则保留证据门控的小幅修正，用于压制 top/bottom 成对尺寸异常。
6. 训练清单支持 train-only 物理标签正规化；val/test 原始人工标注被冻结，不参与修改。

## 3. 数据审计

- 历史人工/反馈数据：1,262 张，其中 train 964、val 138、test 160。
- 官方图清单总计 5,052 条，但本轮训练时 `E:\BaiduNetdiskDownload\pokemon-card-images.zip` 未挂载，因此没有伪装成完整官方图重训。
- 完整官方图分析历史结果：35,186 张解码，6,422 张通过多方法一致性筛选。
- 人工标注统计与 58×83 mm 相符：1,262 张人工数据平均约 578.64×830.52 px；6,422 条官方一致性标签中位数约 580.39×830.77 px。

## 4. 锁定验证结果

正式生产权重保持 `inner_frame_edge_refiner_v5_candidate.pt` 不变；本轮只提升物理门控。

| 端到端指标 | 物理约束仅审查 | v0.8.5 纵向证据门控 | 结果 |
|---|---:|---:|---|
| 验证集整图四边平均 MAE | 1.4577 px | 1.4526 px | 改善 |
| 锁定测试整图四边平均 MAE | 1.3143 px | 1.3101 px | 改善 |
| 锁定测试整图 MAE P95 | 2.9950 px | 2.9950 px | 持平 |
| 锁定测试最坏单边均值 | 2.5481 px | 2.5431 px | 改善 |
| 锁定测试最坏单边最大值 | 14.4170 px | 13.9290 px | 改善 |
| 锁定测试 left/right 改动数 | 0 | 0 | 无横向风险 |

成对 bootstrap 显示点估计方向为正，但置信区间仍跨 0；因此该功能按“保守、小幅、证据门控”方式上线，不扩大修正幅度。

## 5. 被否决的候选

- 物理标签微调网络在独立局部定位测试上可将 MAE 1.298 降至约 1.211 px、P95 4.279 降至约 3.462 px。
- 但接入完整 YOLO、top/left 专家、全局假设和现有门控后，整图 MAE 从 1.316 上升至 1.376–1.555 px。
- 因此所有新 refiner 权重和插值权重均未替换生产权重。
- 同时启用横纵物理坐标修正时，横向锁定测试为 0 胜 / 3 负，所以横向改为 shadow-only。

## 6. 对外框的作用边界

58×83 mm 可以验证外框透视校正是否合理：若校正后的内框尺寸严重偏离 580×830 px，说明外框可能截卡、吸附阴影，或内框本身失败。正式版会把这种情况送复核。

它不能单独决定应移动左、右、上、下哪一条外边，因为卡牌实际居中偏差会改变四侧留白分配。没有独立外缘证据时用固定尺寸强拉外框，会把真实居中偏差误当成外框错误。因此本轮没有加入盲目的外框缩放，只把物理残差作为跨阶段质量门。

## 7. 工程改进

- 训练采样从“所有边完全打乱”改为“图片块之间随机、同图四边连续”，避免 ZIP 图像每轮重复解压约四次。
- 964 张历史训练集单 epoch 已稳定在约 114 秒（RTX 4060 Laptop GPU）。
- 增加可用数据过滤、局部定位评估、权重插值、端到端成对比较和 bootstrap 工具。
- 全部 22 项单元/平台/预裁切/外框恢复回归通过。

## 8. 后续完整重训条件

重新连接包含 `pokemon-card-images.zip` 的 E 盘后，可用已生成的 6,314 行组合清单进行完整官方图微调；仍必须冻结 val/test，并在完整端到端回放同时改善均值、P95、最大误差和各数据源后才能替换权重。

## 9. 参考方法

- Spatial Transformer Networks, NeurIPS 2015：透视/几何标准化。
- HAWP, CVPR 2020；SOLD², CVPR 2021；DeepLSD, CVPR 2023；ScaleLSD, CVPR 2024：结构化线段与跨尺度边界检测。
- Boundary IoU, CVPR 2021；InverseForm, CVPR 2021：边界敏感评价和几何对齐损失。

这些方法在当前系统中的落地原则是：深度模型负责候选与鲁棒表征，亚像素/分段边缘证据负责精修，物理尺寸只做受控先验与失败审查，不能替代视觉证据。
