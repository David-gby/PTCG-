# CardScope 内框超精度优化报告（v0.8.6）

## 结论

在企业外框裁剪器或官方卡图提供可信外框坐标后，内框不再由四条边独立自由移动，而改为：

1. 现有 YOLO 分割、通用边缘精修器和上下/左侧专用器联合估计内框中心；
2. 在 2 倍分辨率矫正图上记录十段长边连续性证据；
3. 以企业确认的外框 63×88 mm、印刷内框线内沿 58×83 mm 严格锁定左右和上下边间距；
4. 官方参考图存在时，再由特征 RANSAC、相位相关和 ECC 产生独立亚像素中心估计；
5. 普通旧照片因外框尺度不确定，不启用强约束，继续使用原有保守门控。

400 张锁定官方测试图的物理规格指标：

| 指标 | v0.8.5 | v0.8.6 | 改善 |
|---|---:|---:|---:|
| 四边平均绝对误差 | 2.184 px | 0.223 px | 89.8% |
| 单图平均误差 P95 | 9.930 px | 0.575 px | 94.2% |
| 最差单边误差 P95 | 11.115 px | 0.766 px | 93.1% |
| 单图平均误差最大值 | 13.588 px | 1.165 px | 91.4% |

加入 160 张人工历史测试图后的 560 张规格一致性复算，平均误差由 2.142 px 降为 0.395 px，P95 由 9.580 px 降为 1.124 px。历史长尾来自旧外框尺度和早期标注口径，因此不能作为可信裁剪分支的硬几何真值。

## 历史实拍人工标注复核

进一步审计 160 张非官方来源测试图后，其中 65 张属于 `manual_v2_precision` 标准卡图/精度集；真正的旧实拍来源为 `human_feedback`、`human_feedback_hard`、`jiance_20260716` 和 `xiuzheng_20260713`，共 95 张。按原人工修正线而不是按物理规格重算后的结果如下：

| 历史实拍 95 张 | 普通保守路径 | 强制当作可信裁剪 | 结论 |
|---|---:|---:|---|
| 单图四边 MAE | 1.382 px | 2.158 px | 强制物理锁定回退 |
| 单图 MAE P95 | 3.177 px | 4.094 px | 强制物理锁定回退 |
| 最差单边 P95 | 6.504 px | 7.002 px | 强制物理锁定回退 |

这项负向消融说明：旧实拍图的外框矫正尺度和旧人工标注口径并不满足“外框坐标绝对可信”的前提，不能因为画面看起来像满框卡图就无条件套用 58×83 mm 硬约束。当前生产实现因此只对官方全框图、被外框检测器严格确认的企业裁剪，以及先通过独立内框几何确认的预裁图启用强分支；普通旧照片保留原保守路径。普通路径中，下边平均误差 1.546 px 仍高于左 1.140 px、上 1.382 px 和右 1.459 px，是下一批实拍训练数据的首要优化方向。

人工标注对比明细：`official_corpus_work_20260816/inner_precision_v086/historical_real160/human_annotation_comparison_real_photos95.json`。

## 关键消融

- `±5 px` 自由梯度中心搜索被否决：会被标题、商标、文字和画面直线吸引。
- 固定深度模型中心、只锁定物理跨度被采用：官方测试中心的横向绝对误差为 0.199 px，纵向为 0.248 px。
- 原图、变暗、变亮三视图中位数仅把平均误差从 0.216 px 降到 0.211 px，但推理开销约为三倍，未进入生产。
- 本轮未替换模型权重。此前物理标签微调虽改善单独边缘网络，却使完整流水线回退；本轮收益来自更合理的坐标系、结构约束和参考图融合。
- 同一张标准图、模型已热启动的内框基准中，普通分支约 0.208 s，2× 高分辨率可信分支约 0.448 s；增加约 0.24 s，用于十段长边质量证据和精度审计。

## 文献与开源方法映射

- DeepLSD：采用“学习语义鲁棒性 + 传统几何定位精度”的混合思想；本项目保留深度中心估计，用解析物理跨度收尾。
- SOLD² / HAWP：采用长线段、多段一致性和遮挡鲁棒性思想，避免局部商标决定整条边。
- PointRend：借鉴只在稀疏边界区域提高采样分辨率的思想，精修图为 2× 分辨率而非全模型整体放大。
- DARK：借鉴坐标解码阶段本身会显著影响定位精度的结论，本轮重点修正最终坐标解码而非盲目扩大模型。
- Boundary IoU：评估使用边缘像素误差、P95 和最差边，不再以对细微边界偏移不敏感的 Mask IoU 作为主要指标。
- Spatial Transformer Networks：透视矫正先建立标准坐标面，后续几何约束只在可信外框坐标系启用。

## 验证产物

- `official_corpus_work_20260816/inner_precision_v086/trusted_physical_comparison_test400.json`
- `official_corpus_work_20260816/inner_precision_v086/trusted_physical_comparison_all560.json`
- `official_corpus_work_20260816/inner_precision_v086/joint_fixed_center_test_official100_multiview/summary.json`
- `work/inner_precision_v086/official_e2e_final/result.json`

端到端官方 JPG 测试成功，输出跨度为 580.000×829.999 px，并标记 `trusted_outer_highres_joint_refinement`。

最终回归测试共 26 项全部通过，覆盖可信/非可信外框分流、58×83 mm 物理跨度、预裁卡恢复、官方参考图融合、平台训练池导出及永久删除等既有功能。

## 部署注意

- 新强约束只应对企业外框裁剪器输出、官方全框图或经过严格满框确认的图片启用。
- 外框裁剪器应尽量裁到真实卡牌物理边界，不保留白底、阴影、卡套或保护壳边缘。
- 若企业裁剪器能在接口元数据里明确传入 `outer_crop_trusted=true`，后续可进一步取消自动判别的不确定性。
- 正式上线后应把新反馈按“可信裁剪 / 普通照片”分开统计，并继续关注内框中心误差 P95 和最差值。
